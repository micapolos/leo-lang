package leo14.typed.compiler

import kotlin.test.Test

class LeoTokenizerTest {
	@Test
	fun tokenize() {
//		processorString {
//			map<String, Syntax> { it.toString() }.process(emptyLeo.parse(token(begin("foo"))))
//		}.assertEqualTo("foo(")
	}
}