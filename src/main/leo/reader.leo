define(it(leo(reader)read(byte(bit(zero)bit(zero)bit(zero)bit(zero)bit(one)bit(zero)bit(one)bit(zero))))is(leo(reader)))
define(it(leo(reader)read(byte(bit(zero)bit(zero)bit(one)bit(zero)bit(zero)bit(zero)bit(zero)bit(zero))))is(leo(reader)))
define(it(leo(reader)read(byte(bit(zero)bit(zero)bit(zero)bit(zero)bit(one)bit(zero)bit(zero)bit(one))))is(leo(reader)))
