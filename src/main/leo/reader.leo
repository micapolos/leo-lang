define(it(leo(reader(core))read(byte(bit(zero)bit(zero)bit(zero)bit(zero)bit(one)bit(one)bit(zero)bit(one))))is(leo(reader(core))))
define(it(leo(reader(core))read(byte(bit(zero)bit(zero)bit(one)bit(zero)bit(zero)bit(zero)bit(zero)bit(zero))))is(leo(reader(core))))
define(it(leo(reader(core))read(byte(bit(zero)bit(zero)bit(zero)bit(zero)bit(one)bit(zero)bit(zero)bit(one))))is(leo(reader(core))))
